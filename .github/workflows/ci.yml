name: CI/CD Pipeline

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      # steps go here


  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      # steps go here


  coverage:
    needs: test
    runs-on: ubuntu-latest
    steps:
      # steps go here


  lint:
    needs: coverage
    runs-on: ubuntu-latest
    steps:
      # steps go here


  format:
    needs: lint
    runs-on: ubuntu-latest
    steps:
      # steps go here


  security:
    needs: format
    runs-on: ubuntu-latest
    steps:
      # steps go here


  deploy:
    needs: security
    runs-on: ubuntu-latest
    steps:
      # steps go here
